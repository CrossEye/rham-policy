{
  "name": "RHAM_Policies",
  "version": "0.7.3",
  "description": "Online version of RHAM's Policy Manual",
  "main": "index.js",
  "scripts": {
    "pkg": "cross-var echo Package $npm_package_version",
    "build": "cross-env TIDDLYWIKI_PLUGIN_PATH=./plugins cross-var node scripts/updateVersion latest && cross-env TIDDLYWIKI_PLUGIN_PATH=./plugins tiddlywiki . --output docs/latest --build index",
    "release": "cross-env TIDDLYWIKI_PLUGIN_PATH=./plugins cross-var node scripts/updateVersion $npm_package_version && cross-env TIDDLYWIKI_PLUGIN_PATH=./plugins tiddlywiki . --output docs --build index && cross-var shx mkdir -p docs/$npm_package_version && cross-var shx cp docs/index.html docs/$npm_package_version/index.html",
    "start": "cross-env TIDDLYWIKI_PLUGIN_PATH=./plugins tiddlywiki . --listen port=7425"
  },
  "keywords": [
    "TiddlyWiki",
    "SQL"
  ],
  "author": "Scott Sauyet",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CrossEye/rham-policy/issues"
  },
  "homepage": "https://github.com/CrossEye/rham-policy/#readme",
  "dependencies": {
    "tiddlywiki": "^5.3.8"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "cross-var": "^1.1.0",
    "shx": "^0.3.4"
  }
}
